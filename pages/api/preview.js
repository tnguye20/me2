// import {
//   getAllSkills,
//   getAllProjects
// } from '../../lib/cms';

export default async function handler(req, res) {
    // const [skills, projects] = await Promise.all([getAllSkills(true), getAllProjects(true)]);
    // res.setPreviewData({});
    // res.json({
    //   skills,
    //   projects
    // });
}
